# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build api-express-esbuild`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t api-express-esbuild`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system api-express-esbuild && \
          adduser --system -G api-express-esbuild api-express-esbuild

COPY dist/apps/api-express-esbuild api-express-esbuild
RUN chown -R api-express-esbuild:api-express-esbuild .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix api-express-esbuild --omit=dev -f install

CMD [ "node", "api-express-esbuild" ]
