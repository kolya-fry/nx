# lib-js-tsc

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build lib-js-tsc` to build the library.

## Running unit tests

Run `nx test lib-js-tsc` to execute the unit tests via [Jest](https://jestjs.io).
